import { AdminStub } from "@/components/AdminStub";

export default function RecordsPage() {
  return <AdminStub title="Records" blurb="Raw ENS records — part of the full kit, not used in this demo build." />;
}
