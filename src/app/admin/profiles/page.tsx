import { AdminStub } from "@/components/AdminStub";

export default function ProfilesPage() {
  return <AdminStub title="Profiles" blurb="ENS subdomain / profile management — part of the full kit, not used in this demo build." />;
}
