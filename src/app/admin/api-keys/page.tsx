import { AdminStub } from "@/components/AdminStub";

export default function ApiKeysPage() {
  return <AdminStub title="API Keys" blurb="External API-key management — part of the full kit, not used in this demo build." />;
}
