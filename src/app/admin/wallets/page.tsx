import { AdminStub } from "@/components/AdminStub";

export default function WalletsPage() {
  return <AdminStub title="Wallets" blurb="Claim & wallet management — part of the full kit, not used in this demo build." />;
}
