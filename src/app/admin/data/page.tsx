import { AdminStub } from "@/components/AdminStub";

export default function DataPage() {
  return <AdminStub title="Data" blurb="ENS record data pipeline — part of the full kit, not used in this demo build." />;
}
