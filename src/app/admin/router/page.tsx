import { AdminStub } from "@/components/AdminStub";

export default function RouterPage() {
  return <AdminStub title="Gateway / Router" blurb="Gateway / mesh-router controls — part of the full kit, not used in this demo build." />;
}
